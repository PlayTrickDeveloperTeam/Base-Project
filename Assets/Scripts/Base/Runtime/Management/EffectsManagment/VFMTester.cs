using UnityEngine;
public class VFMTester : MonoBehaviour {
    private void Start() {
        // for (int i = 0; i < 50; i++) 
        // EffectsManager.SpawnAParticle(Enum_Particles.SphereExpo, Vector3.zero).SetLoop(5);
    }
}